Write a program to accept list from user and print element by element in reverse order using recursion.
to accept two list from user sort them and merge the two sorted list element by element presevring the sort order