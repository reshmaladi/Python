#!C:\Python34

import math
def prime(no):
	mat.sqrt(no)
	
def getPrimes(inputlist):
		for number in inputlist:
			if ISPrime(number):
				yield number
				
def main():
	x =GetPrimes(range(1,10))
	print (type(x))
	print (next(x))
					